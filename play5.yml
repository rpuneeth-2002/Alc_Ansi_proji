---
- name: demonstrate file module
  hosts: localhost
  become: true
  gather_facts: false
  become_user: root
  tasks:
   - name: Create a Directory
     file: 
      path: /etc/picsDir
      state: directory
      #state: absent
      owner: root
      group: ansible
      mode: '0750'